apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "energy-saving.fullname" . }}-config
  labels:
    {{- include "energy-saving.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- toYaml .Values.appConfig | nindent 4 }}
